PORT=4000
MONGODB_URI=
JWT_SECRET=change-me-to-a-long-random-string-at-least-32-chars
CORS_ORIGIN=http://localhost:3000
NODE_ENV=development
# Optional: when set, post/avatar images upload to ImgBB (HTTPS URLs). Recommended for Vercel.
# IMGBB_API_KEY=
